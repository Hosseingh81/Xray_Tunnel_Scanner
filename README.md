
# Xray Tunnel Scanner (Powered by iPerf3)

این پروژه یک ابزار پیشرفته برای اسکن و یافتن آی‌پی‌های تمیز (Clean IPs) جهت استفاده در کانفیگ‌های Xray است.

تفاوت اصلی این اسکنر با نمونه‌های دیگر در شیوه تست آن است. این ابزار به جای پینگ ساده، ترافیک واقعی را با استفاده از **iPerf3** از داخل تانل عبور می‌دهد. این کار باعث می‌شود تا کیفیت واقعی خط (شامل سرعت دانلود/آپلود، پکت‌لاس و تاخیر) در شرایط واقعی سنجیده شود.

## ✨ قابلیت‌ها

*   **تست واقعی تانل:** عبور دادن ترافیک از پورت `dokodemo-door` برای اطمینان از عملکرد پروکسی.
*   **پشتیبانی از تمام پروتکل‌ها:** سازگار با `VLESS`, `VMess`, `Trojan` و `Shadowsocks`.
*   **مدیریت هوشمند SNI:** جلوگیری از خطای TLS با حفظ `Host` و `SNI` اصلی کانفیگ هنگام تغییر آی‌پی.
*   **کنترل کامل:** امکان توقف موقت (Pause)، ادامه (Resume) و ذخیره اضطراری حین اسکن.
*   **خروجی دقیق:** ارائه نتایج در سه فایل جداگانه (مرتب شده بر اساس سرعت، تاخیر و پکت‌لاس).
*   **پشتیبانی از iPerf:** امکان اجرای تست‌های UDP، تست دانلود (Reverse) و تست‌های طولانی مدت.

---

## 📂 ساختار فایل‌ها

برای اجرای صحیح برنامه، فایل‌های پروژه باید به صورت زیر مرتب شده باشند:

```text
Project-Folder/
├── scanner.py          # فایل اصلی برنامه (Python Script)
├── config.json         # کانفیگ پایه Xray (ورودی شما)
├── ranges.txt          # لیست رنج‌های آی‌پی برای اسکن
├── xray.exe            # هسته اجرایی Xray
└── iperf/              # پوشه iPerf
    └── iperf3.exe      # فایل اجرایی iPerf
```
🚀 راهنمای نصب و اجرا
۱. نصب پیش‌نیازها
ابتدا پایتون (نسخه ۳.۸ یا بالاتر) را نصب کنید. سپس دستور زیر را در ترمینال اجرا کنید تا کتابخانه‌های مورد نیاز نصب شوند:

bash
pip install requests pysocks
۲. تنظیم ورودی‌ها
فایل config.json: کانفیگ سالم خود را (که از نرم‌افزارهایی مثل v2rayN یا Nekoray گرفته‌اید) در پوشه قرار دهید.

فایل ranges.txt: لیست رنج‌های آی‌پی (CIDR) را در این فایل ذخیره کنید.

مثال محتوای ranges.txt:

```text
104.16.0.0/24
1.1.1.1/32
```
۳. اجرای اسکنر
دستور زیر را در ترمینال یا CMD اجرا کنید:

```bash
python scanner.py
```
۴. وارد کردن دستور تست (اختیاری)
پس از اجرا، برنامه از شما دستور iPerf را می‌خواهد. می‌توانید Enter بزنید تا تست پیش‌فرض انجام شود، یا دستورات خاص خود را وارد کنید.

مثال برای تست پکت‌لاس (UDP):

```text
-c ping.online.net -p 5200 -u -b 1M
```
مثال برای تست طولانی (۶۰ ثانیه):

```text
-t 60
```
🎮 کلیدهای کنترلی
در حین انجام اسکن می‌توانید از کلیدهای زیر استفاده کنید:

P : توقف موقت اسکن (Pause).

S : ادامه اسکن (Resume).

Ctrl + C : توقف کامل اسکن و ذخیره نتایج به دست آمده تا آن لحظه.

📊 خروجی‌ها
نتایج اسکن در پوشه clean_ips ذخیره می‌شوند. برای راحتی شما، نتایج در ۳ فایل دسته‌بندی می‌شوند:

sorted_by_speed.json:

آی‌پی‌ها با بیشترین سرعت 

sorted_by_loss.json:

آی‌پی‌ها با کمترین پکت‌لاس 

sorted_by_delay.json:

آی‌پی‌ها با کمترین تاخیر یا Ping


(باید ذکر شود که کد این اسکنر به طور کامل توسط هوش مصنوعی نوشته شده است)
